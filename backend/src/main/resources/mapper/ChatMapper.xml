<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.demo.mapper.ChatMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_message (from_user_id, to_user_id, content, msg_type, create_time, message_id)
        VALUES (#{fromUserId}, #{toUserId}, #{content}, #{msgType}, #{createTime}, #{messageId})
    </insert>

    <select id="selectHistory" resultType="com.project.demo.entity.ChatMessage">
        SELECT * FROM (
            SELECT * FROM chat_message
            WHERE from_user_id = #{userId} AND to_user_id = #{friendId}
            UNION ALL
            SELECT * FROM chat_message
            WHERE from_user_id = #{friendId} AND to_user_id = #{userId}
        ) t
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

</mapper>
